# Set VITE_CODESPACE_NAME to your GitHub Codespace name to connect the frontend to the backend.
# Example: VITE_CODESPACE_NAME=my-codespace-name
# If unset, the frontend safely falls back to localhost:8000.
VITE_CODESPACE_NAME=
